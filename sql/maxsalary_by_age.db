SELECT CONCAT(FLOOR(YEAR(birth_date) / 10) * 10, 's') AS decade,
  MAX(salary) AS max_salary
FROM employees e
  JOIN salaries s ON e.emp_no = s.emp_no
  AND s.to_date = '9999-01-01'
GROUP BY decade
ORDER BY decade DESC
